name: drumio_mobile
description: "drumio mobile — bateria acústica em primeira pessoa, port do drumio web."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  # Engine de áudio C++ (miniaudio) com latência baixa — essencial pra bateria
  flutter_soloud: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/
